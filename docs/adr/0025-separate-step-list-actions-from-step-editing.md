# Separate Step list actions from Step editing

Keep completion and drag reordering on the compact Step list, while opening a dedicated Step editor for text, date, and deletion. This gives the parent Task editor Task-list-like density and clear boundaries, avoids repeating completion and ordering controls in two places, and keeps destructive deletion behind an intentional edit action.
