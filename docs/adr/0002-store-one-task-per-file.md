# Store one task per file

Each task is stored as one Markdown file under the configured task folder. Compared with checkbox lines embedded throughout a vault, this creates more files but gives tasks stable identities, reduces conflicting edits, and provides a durable base for later list, calendar, and Kanban projections.
