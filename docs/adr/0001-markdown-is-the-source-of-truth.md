# Markdown is the source of truth

Task data lives in the user's Obsidian vault rather than a plugin database or hosted service. This keeps the product local-first, makes agent changes inspectable, and avoids operating a synchronization backend; users remain responsible for synchronizing their vault between devices.
