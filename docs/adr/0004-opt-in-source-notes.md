# Select source notes with explicit opt-in rules

Codex may extract tasks only from folders explicitly selected in the Obsidian plugin or notes explicitly marked with `taskmate-source: true`; `taskmate-source: false` excludes a note inside a selected folder. Folder rules avoid repetitive file moves and mass frontmatter changes while retaining user control over the AI's scope.
